apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: system-api
  labels:
    app.kubernetes.io/name: system-api
spec:
  hosts:
    - system-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: system-api
            port:
              number: 80
          weight: 100
