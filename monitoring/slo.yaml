apiVersion: sre.platform.ohanyere.io/v1alpha1
kind: ServiceLevelObjective
metadata:
  name: system-api-availability
  labels:
    app.kubernetes.io/name: system-api
    finops.ohanyere.io/cost-center: developer-experience
spec:
  service: system-api
  owner: Developer Experience
  objective: 99.9
  window: 30d
  indicator:
    type: availability
    goodEvents: sum(rate(http_requests_total{service="system-api",status!~"5.."}[5m]))
    totalEvents: sum(rate(http_requests_total{service="system-api"}[5m]))
  alerting:
    burnRate:
      enabled: true
