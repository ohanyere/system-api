apiVersion: storage.platform.ohanyere.io/v1alpha1
kind: S3BucketClaim
metadata:
  name: system-api-bucket
  labels:
    app.kubernetes.io/name: system-api
    finops.ohanyere.io/cost-center: developer-experience
spec:
  environment: dev
  versioning: true
  encryption: platform-managed
  lifecycle:
    transitionToInfrequentAccessDays: 30
